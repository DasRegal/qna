$('.notice').html("<%= notice %>");
$('.alert').html("<%= alert %>");

<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |msg| %>
    $('#answer_validations_errors').append('<p><%= msg %>');
  <% end %>
<% else %>
  $('.answers').html('<%= j render @answer %>');
<% end %>